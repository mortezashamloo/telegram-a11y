# 08 – App display name

| Locale | Name |
|--------|------|
| English (default) | Telegram Accessible |
| Persian (`values-fa`) | تلگرام دسترس‌پذیر |

Applied automatically in GitHub Actions by rewriting `AppName` in `strings.xml`.

**Not** related to my.telegram.org API app title.

Custom launcher icon: deferred until user provides an image.
