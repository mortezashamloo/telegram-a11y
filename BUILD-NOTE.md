# Next build

Workflow is `workflow_dispatch` only.

To build with AppName **Telegram Accessible** / **تلگرام دسترس‌پذیر**:

1. Open Actions → Build APK
2. Run workflow

Previous successful APK (without rename): run 32619630369
